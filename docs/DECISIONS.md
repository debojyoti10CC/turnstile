# Decisions

| Date | Decision | Alternatives | Reason |
|---|---|---|---|
| 2026-09-11 | Channel boxes are never deleted | Delete on close + tombstone | Deleting resets totalClaimed → replay of old vouchers on re-fund. Salt gives fresh channels |
| 2026-09-11 | claim = accounting, settle = sweep | Transfer per claim | Mirrors EVM binding; one transfer for many channels |
| 2026-09-11 | Stale claim rows are no-ops | Revert | Idempotent settler retries; one bad row shouldn't sink a batch |
| 2026-09-11 | Withdraw delay in seconds (latest_timestamp), 900–2,592,000 | Rounds | Same units/bounds as EVM & SVM bindings |
| 2026-09-11 | payerAuthorizer may also start/finalize withdrawals | Payer only | Funds always go to payer, so hot key gains no theft path; lets agents exit without the cold key |
| 2026-09-11 | No voucher expiry | expiresAt field | SVM binding requires expiresAt = 0 for the same reason: withdraw delay already bounds redemption |
| 2026-09-11 | escrow-client/tests build every app-call's box/account/asset references explicitly and disable algokit_utils's simulate-based auto-population (`populate_app_call_resources`/`cover_app_call_inner_transaction_fees: False`) | Rely on `allowUnnamedResources` auto-discovery per CLAUDE.md §7 | On this algod 4001/algokit_utils 4.2.3 pairing, letting the composer auto-discover resources for an ABI *method call* (as opposed to a bare app call) intermittently routes through an internal fee-probe codepath that submits a synthetic create+delete "op-up" stub transaction and misreports a `group fee too small` error instead of the real missing-resource error. Explicit refs are deterministic and match what a production facilitator would compute anyway (CLAUDE.md already calls for simulate-based *discovery*, not blind reliance at submit time) |
| 2026-09-11 | `claim()`'s extra fee is computed as `ceil(n * 2600 / 700) * minFee` | Flat `1 * minFee` guess | Matches CLAUDE.md §1's op-up formula exactly: n=1 row needs `ceil(2600/700)=4` op-up inner transactions, each consuming one network min-fee from the pooled group credit. Verified empirically on LocalNet — 3 op-ups (3000 µAlgo) under-funds and fails with `group fee too small`; 4 (4000 µAlgo) succeeds |
| 2026-09-11 | LocalNet dev-mode timestamp-offset tests submit one throwaway 0-amount payment immediately after `algod.set_timestamp_offset()` before the time-sensitive call | Call the time-sensitive method directly after setting the offset | Empirically, algod only applies a newly-set dev-mode offset starting from the block *after* the one it was set on — the very next submitted transaction still sees the old effective timestamp. A harmless "latch" transaction (unique note, to avoid `TransactionPool.Remember: transaction already in ledger` on repeat calls) forces that extra block so the following call sees the advanced clock |
| 2026-09-11 | `packages/x402-avm-batch` pins `@algorandfoundation/algokit-utils` to the stable ^9.2.2 line (via `@turnstile/escrow-client`), not the `10.0.0-alpha.46` version `.refs/x402`'s own `@x402/avm` package uses | Match `@x402/avm`'s exact algokit-utils version per CLAUDE.md §3 ("match the major used by @x402/avm") | `@x402/avm`'s `signer.ts`/`utils.ts` depend on subpath exports (`/algorand-client`, `/crypto`, `/transact`, `/common`) that only exist on algokit-utils's unreleased "decoupling" alpha branch — no stable release, undocumented, breaking-prone. `@turnstile/escrow-client` is already built, tested against real LocalNet transactions, and pinned to the stable 9.2.2 API; `@x402/core`'s `SchemeNetworkClient`/`SchemeNetworkServer` interfaces (what we actually implement) are stable at 2.25.0 and carry no algokit-utils version coupling, so there is no real requirement to match `@x402/avm`'s internal dependency choice. Logged here as a deliberate, documented deviation rather than a silent drift |
| 2026-09-11 | P2 (`x402-avm-batch`) scope for this pass: implement `ChannelStorage` (in-memory, per-channel async lock), a channel manager with the exact charge/signed-max rule checks (I9), server `SchemeNetworkServer` hooks for verify/settle, and a client `SchemeNetworkClient` for payload creation + corrective-402 adoption with signature re-verification (I11). Skip the EVM reference's `pendingRequest`/TTL reservation system and its auto-settlement interval runner | Port every EVM file line-for-line (channel manager's auto claim/settle/refund loop, TTL-bounded pending-request reservations, ~600-line verify.ts/settle.ts) | The EVM reference is ~40 files / 3,000+ lines for client+server alone, much of it either P4's concern (the settler's claim/settle/refund loop lives in EVM's `channelManager.ts`, which this repo's phase plan assigns to `packages/settler`) or a throughput optimization (the pending-request reservation avoids holding a lock across arbitrary handler execution time, but isn't required for *correctness*: a single atomic `storage.updateChannel` at charge-commit time that rechecks `newCharged <= signedCap` and `<= mirrored balance` preserves I9/I12 without it, at the cost of rejecting overlapping same-channel requests only after their handler runs instead of before). Revisit the reservation system if the no-reservation approach proves a measurable bottleneck under the P3 demo's concurrency. |
| 2026-09-11 | P3 demo: the client/agent submits its own deposit transaction group directly (via `@turnstile/escrow-client`, its own keys) before sending the x402 payload; the facilitator's `verify()` for a deposit payload only confirms it already landed on-chain (via the channel manager's cold-start mirror read) rather than building, co-signing, or submitting anything itself | Have the facilitator build+co-sign+submit the deposit group on the client's behalf, accepting partially-signed transaction bytes over the wire per the full spec | Splitting a transaction group across an HTTP trust boundary (client signs the axfer, hands raw bytes to a facilitator that adds its own signature and submits) is real, separate plumbing this pass's budget doesn't cover; the escrow-client tx builders from P1 assume one caller with all needed signers already registered on one `AlgorandClient`. Documented as a real gap for a production facilitator (one that sponsors fees or accepts unsigned groups from clients with no chain access of their own), not a silent shortcut — the demo's agent is assumed to hold funded keys and call the chain directly, which a real end-user wallet-driven agent would not want to do |
| 2026-09-11 | `BatchSettlementAvmScheme.paymentFlows` declares `"authorization"` (verify before handler, one settle call after), matching the EVM batch-settlement reference exactly | `"escrow"` (the name that sounded right for an escrow-channel design) | `@x402/core`'s `"escrow"` payment flow is a *different*, unrelated concept: it calls `beforeSettle`/`afterSettle` **twice** per request (`phase: "before-handler"` then `"after-handler"`), for two-phase settlement schemes. Picking it made our single-shot `beforeSettle` run twice, double-charging the channel (`charging 10050 would exceed signed max 10000` on the very first LocalNet demo run) until caught and fixed. The EVM reference's choice of `"authorization"` for this exact same problem was the tell — scheme *names* in this ecosystem don't always mean what they sound like; check the actual reference before assuming |
| 2026-09-11 | `BatchSettlementAvmClientScheme` signs each new voucher's ceiling off `record.signedMaxClaimable` (the latest *reserved* ceiling, written back to storage synchronously inside a per-channel lock before the request is ever sent), not off `chargedCumulativeAmount` (the confirmed actual total, only updated after a response arrives) | Use `chargedCumulativeAmount` as the signing base (matches it semantically as "how much have I used so far") | Two fields drift apart under dynamic pricing (signed ceiling is always `>=` actual charged) and, more importantly, under concurrency: using the confirmed-actual field as the signing base means two concurrent requests for the same channel can read the same stale base and sign overlapping ceilings, so whichever response lands second gets rejected for exceeding a cap the first already consumed -- reproduced on the demo with `concurrency=5`, 95/200 calls failing. Fixed by (a) using `signedMaxClaimable` as the base and (b) wrapping the read-sign-write sequence in a per-channel async lock (mirroring the server's own `ChannelStorage` lock pattern) so the reservation is atomic with respect to other in-flight requests on the same channel, regardless of how long their network round-trip takes |
| 2026-09-11 | Added a "no new charges once a pending withdrawal is within its safety margin" check to `BatchSettlementChannelManager.verifyVoucher` (rejects with the pre-existing but previously-unused `ERR.withdrawPending`; default margin 60s, configurable via `withdrawSafetyMarginSec`, injectable `now()` for testability) | Leave it unchecked (the contract's own `finalize_withdraw` already bounds how much a payer can ever be shorted, so no fund-safety issue exists) | CLAUDE.md's P5 attack list explicitly calls for "voucher during withdraw window past safety margin" to be rejected, and this was a genuine gap the adversary suite caught: before this change, the server would happily keep charging (and the client keep paying) against a channel whose payer had already signaled intent to exit, which is an availability/trust problem even though funds are never at risk. Found and fixed during P5, not before -- a concrete example of the adversary suite doing its job |
| 2026-09-11 | `packages/adversary`'s LocalNet-bound world disables the `AlgorandClient`'s suggested-params cache (`setSuggestedParamsCacheTimeout(0)`) | Leave the default cache (a few seconds) | The adversary suite fires many structurally-identical transactions back-to-back (e.g. two `settle()` calls with no varying fields). With the default cache, two such calls inside the cache window get the same `firstValid`/`lastValid` and become byte-identical (same-txid) transactions, which algod correctly rejects as `TransactionPool.Remember: transaction already in ledger` -- a harness artifact from hammering the node faster than a real client would, not a protocol issue. Reproduced directly: 2/24 attacks failed with this exact error before the fix, 24/24 passed after |
| 2026-09-11 | The adversary fuzz (`packages/adversary/src/fuzz.ts`) runs deposit/claim/stale-claim/refund/settle interleavings against real LocalNet at 500 steps by default, omitting withdraw/finalize from the op mix | Port the offline pytest fuzz's full op set (which includes withdraw/finalize via a mutable emulator clock) onto LocalNet | Advancing LocalNet's dev-mode clock costs two real transactions per attempt (set offset + a latching transaction, see the dev-mode timestamp-offset decision above), which would make withdraw/finalize disproportionately expensive inside a tight random loop; that path already has dedicated real-node coverage elsewhere (escrow-client's localnet suite for I7, the settler's I8 test), and the offline pytest fuzz (`test_fuzz_conservation`, 300 steps) already exercises it cheaply against the algopy emulator. Measured: 500 steps of the narrower op set complete in ~35-40s against real LocalNet, comfortably meeting CLAUDE.md's "≥500 steps" bar without the clock-advance op |
| 2026-09-11 | `claim()` batch size is capped by box/foreign-reference count (`MAX_APP_CALL_FOREIGN_REFERENCES`=8), exported as `MAX_CLAIM_ROWS_PER_CALL_SAME_RECEIVER`=7 (same receiver across rows) / `MAX_CLAIM_ROWS_PER_CALL`=4 (worst case, every row a different receiver); `claimBatch()` enforces the exact box-count bound rather than a flat row number | Derive the cap analytically from the opcode-budget op-up formula in CLAUDE.md §7 (`ceil(n*2600/700)` op-ups, assumed to exhaust `MAX_TRANSACTION_GROUP_SIZE`=16 around n=4) | That formula's assumption was wrong: empirically on LocalNet, n=7 claim rows (1 shared unsettled box + 7 channel boxes = 8 box refs) succeeds with no opcode-budget issue even at n=30 rows' worth of op-up fee; n=8 rows (9 box refs) fails outright with algod's literal `tx.Boxes too long, max number of box references is 8`. Opcode budget was never the binding constraint — box references were. Verified via `AlgodHTTPError`/algosdk client-side validation, not a contract-level assert, confirming it's a protocol-level txn field limit, not something `ensure_budget`'s op-up mechanism can work around |
| 2026-09-12 | `packages/core`, `packages/escrow-client`, `packages/x402-avm-batch`, `packages/settler` each declare an explicit `exports` map (`"."` only, pointing at `dist/index.{js,d.ts}`) and a `files: ["dist"]` allowlist | Leave package.json without `exports`/`files` (Node falls back to resolving any path under the package root) | A plugin SDK meant to be consumed by other projects (P6's stated devtooling goal) should have one well-defined entry point and never ship source/test files in the published tarball; confirmed via `grep` that nothing in this workspace imports a subpath of any of these four packages, so the restriction breaks nothing internally. Rebuilt (`pnpm -r build`) and re-ran the full suite afterward; all 67 TS/Python tests + the 24/24 adversary report still pass |
| 2026-09-12 | `apps/demo-agent/src/bench.ts`'s "exact" mode is a naive one-real-transaction-per-call baseline (`algorand.send.assetTransfer` direct from payer to receiver), not `@x402/avm`'s real `exact` scheme facilitator | Wire in `@x402/avm`'s actual exact-scheme client/facilitator for the comparison | `@x402/avm` pins `algokit-utils@10.0.0-alpha.46`, already rejected for this workspace (see the algokit-utils pin decision); a naive per-call on-chain transfer is also the fairer comparison for this project's value proposition, since it represents what *any* non-batched, settle-every-request scheme costs on-chain, not one specific facilitator implementation's overhead |
| 2026-09-12 | `bench.ts`'s deposit-sizing multiplier for the batch-settlement run is `ceil(calls * 1.1)`, i.e. proportional to the call count, not a quarter of it | Original `max(250, ceil(calls/4))` | Found via a real run: at N=1000 the batch leg only completed 250/1000 calls before every request failed with `invalid_batch_settlement_avm_cumulative_exceeds_balance`. Root cause is a real protocol property, not a bench artifact: `apps/demo-merchant`'s `/v1/infer` route reserves its full advertised price *ceiling* (1200 atomic units) against the channel balance on every voucher, regardless of actual usage (the client can't know actual usage before the response), so funding N calls needs a deposit of at least `ceiling * N`. The old formula sized the deposit for at most `250` calls' worth of ceiling reservations no matter how large `N` was, which is exactly why `succeeded` landed at precisely 250 |
| 2026-09-12 | `apps/{demo-agent,demo-merchant,facilitator}` pick `AlgorandClient.testNet()` vs `AlgorandClient.defaultLocalNet()` based on a `NETWORK` env var (default localnet) | A `@turnstile/deploy` workspace package per CLAUDE.md's aspirational layout | That package was never built; `root package.json`'s `deploy:testnet` script instead shells out directly to a new `contracts/scripts/deploy_testnet.py` (mirrors the existing `deploy.py` LocalNet pattern). Simpler, and consistent with how every other script in this repo already drives deployment through the Python side |
| 2026-09-12 | `contracts/scripts/deploy_testnet.py` generates a fresh deployer account on first run, writes its mnemonic only to a local gitignored `contracts/.env.testnet`, and never prints it -- it prints only the address and a funding instruction, and exits with `status: "needs_funding"` until the balance clears a minimum | Ask the operator to paste an existing funded mnemonic into a prompt or CLI arg | Keeps a real secret out of shell history, chat transcripts, and any log line, per CLAUDE.md rule 6 ("never log mnemonics... at info level"); the script is idempotent and safe to rerun after funding, and reruns after partial progress (e.g. a stale-round transaction failure) reuse whatever asset/app/sub-accounts are already on record in that file rather than recreating them |
| 2026-09-12 | `packages/settler/test/settler.localnet.test.ts`'s I8 test resets LocalNet's dev-mode timestamp offset back to 0 (with a latching throwaway transaction) after advancing it to simulate the withdraw delay passing | Leave the offset as set | Found by running the full `pnpm -r test && pnpm adversary` sequence end to end: the settler test sets the offset to `withdrawDelaySec + 60` (960 with the default 900s delay) to finalize a withdrawal, but never reset it -- and the offset is **global LocalNet state**, not scoped to the test process. The very next `pnpm adversary` run then saw a virtually-advanced clock and `finalize_before_delay` (an attack that finalizes a withdrawal before its real delay has elapsed) wrongly succeeded, since dev-mode's fake clock really had advanced past 900s. Not a contract or product bug -- `finalize_before_delay` passes 24/24 on a clean LocalNet -- but a real cross-suite contamination bug in test infrastructure, the same class of issue CLAUDE.md's dev-mode-offset guidance already warns about for any test that advances the clock |
| 2026-09-12 | TestNet deployment verified live, not just "deploy succeeded": real deposit group, 3 real paid `/v1/infer` requests, then a real `claim()` + `settle()`, all on Algorand TestNet (app 771555042, asset 771555032, genesis `SGO1GKSzyE7IEPItTxCByw9x8FmnrCDe`) | Stop at "the deploy script exits 0" | A green deploy script proves the app exists on-chain, not that the full x402 batch-settlement flow actually works against real TestNet round timing (which is ~2.8s/round vs LocalNet's instant dev-mode, and did surface one real bug during deploy itself -- see the stale-suggested-params note below). Confirmed: channel balance 300000, `chargedCumulativeAmount` on the agent matched the voucher, and after claim+settle `totalClaimed` landed at exactly 3600, matching the signed voucher |
| 2026-09-12 | `deploy_testnet.py`'s asset/app-opt-in/sub-account-funding steps each check on-chain state before acting (e.g. "is this asset ID already opted into by the app/account") rather than assuming a clean slate | Always perform every step unconditionally | A real TestNet round takes ~2.8s to finalize, long enough that algokit-utils' suggested-params cache produced a `TransactionPool.Remember: txn dead: round X outside of Y--Z` failure partway through the first real run (LocalNet's instant dev-mode never surfaces this). Making each step idempotent means a partial-failure rerun (the common case against a real, slower network) just skips what already landed instead of erroring on "already opted in" |
| 2026-09-12 | `apps/dashboard` (React 19 + Vite) reads three new read-only `/debug/*` endpoints added to `apps/demo-merchant` (`/debug/channels` lists every channel from the same `ChannelStorage` the server already uses; `/debug/bench` and `/debug/adversary-report` just read `apps/demo-agent/bench.json` / `packages/adversary/report.json` off disk) rather than talking to a dedicated settler/dashboard-API service | Build the standalone dashboard-API service CLAUDE.md's layout implies (polling "merchant + settler") | No standalone settler process exists yet (P4 built `@turnstile/settler` as a library, exercised only in its own tests) -- see the P6 TestNet progress entry. Reusing the merchant's own in-memory channel storage for `/debug/channels` means the dashboard shows exactly what the payment middleware sees, with zero duplicated state; the bench/adversary endpoints just expose files those CLIs already produce, so the dashboard never fabricates a number |
| 2026-09-12 | `packages/x402-avm-batch`'s new `SqliteChannelStorage` (closing the P2/§6 gap: only `InMemoryChannelStorage` existed before) is built on Node's built-in `node:sqlite` (`DatabaseSync`), loaded via `createRequire(import.meta.url)('node:sqlite')` rather than a static `import ... from 'node:sqlite'` | `better-sqlite3`, as CLAUDE.md §3 specifies | `better-sqlite3@13.0.3` has no prebuilt binary for this machine's Node 24/win32-x64 combination and its source build failed outright (`node-gyp rebuild` error MSB8020: "the build tools for ClangCL... cannot be found" -- this machine's Visual Studio Build Tools install doesn't include the ClangCL component `better-sqlite3`'s current binding.gyp requires). Rather than requiring an extra system-level Visual Studio component install (out of scope for a repo-level fix, and not guaranteed to be available in every dev/CI environment), switched to `node:sqlite`, which ships in Node itself (stable, no native compile, no extra dependency) and offers the same synchronous, transactional API `better-sqlite3` would have. The `createRequire` indirection works around a separate, unrelated issue: Vite's SSR module graph (which `vitest` uses to run this package's tests) doesn't yet recognize `node:sqlite` as a builtin and tries to resolve it as a nonexistent npm package named `sqlite`; `require()` bypasses Vite's resolution and hits Node's real loader directly. Schema matches CLAUDE.md §6 exactly; every `updateChannel` mutation runs inside one `BEGIN IMMEDIATE` transaction, which (per SQLite's own file locking) keeps I9/I12's atomicity guarantee even across multiple OS processes sharing one database file, not just within one JS runtime. 6 new tests (`test/sqliteStorage.test.ts`) cover create/update/unchanged/delete, cross-instance persistence after reopening the file, and 20 concurrent same-channel updates never losing an increment |
| 2026-09-12 | Added `apps/settler` (`@turnstile/settler-service`) -- a standalone long-running process wrapping `packages/settler`'s already-tested `Settler` class -- closing the P4 gap where the settler existed only as a library exercised by its own tests | Leave the settler as a library forever, relying on someone manually calling `tick()` | CLAUDE.md's own P4 write-up and I8 assume a settler that polls continuously and independently of the merchant process; a library nobody instantiates as a service can't do that in a real deployment. The new process reads the same on-disk `SqliteChannelStorage` file the merchant writes to via `CHANNEL_DB_PATH` (SQLite's file locking makes single-writer-at-a-time sharing safe), so it sees exactly the vouchers the merchant accepted, with zero duplicated state. Verified live on LocalNet in this session, not just built: a fresh deploy, a merchant started with `CHANNEL_DB_PATH` set, 15 real paid `/v1/infer` calls through the agent, then `apps/settler` started as a *separate* OS process with a low claim/settle threshold -- it claimed 18,000 atomic units and settled 18,000 atomic units in two real on-chain transactions on its own poll timer, with no manual trigger |
| 2026-09-12 | Fixed `apps/dashboard`'s channel-table "Exposure" column to `chargedCumulativeAmount - totalClaimed` (what the receiver has actually earned but not yet claimed on-chain), and added a network-name badge (MainNet/TestNet/LocalNet) to the header | Leave Exposure as `signedMaxClaimable - totalClaimed` (the pre-authorized ceiling, already shown in its own column) | User-reported bug: the column was silently duplicating "Signed max" minus claimed instead of showing real receiver risk, which for dynamic pricing (client can't know actual usage in advance, so `signedMax` is always >= `charged`) understated how much was actually safe to have claimed vs. overstated pending exposure. Also replaced the CSS-div "bars" hack in `BenchPanel` with a proper SVG grouped bar chart (`BarChart.tsx`) covering fees, on-chain txn count, and p95 latency, all batch-vs-exact by N; ran the `dataviz` skill's palette validator before picking colors -- the pre-existing green/red pair (`#2ea96f`/`#c96464`) failed the colorblind-separation check (deuteranopia ΔE 2.6, far under the ΔE>=8 target), so switched to a validated blue/orange pair (`#4a8fc7`/`#d1782e`, all six checks pass in dark mode) |
| 2026-09-12 | Added `scripts/gen-bench-charts.mjs`, generating static SVG bar charts from the real `apps/demo-agent/bench.json` into `docs/assets/` and embedding them in the README's Benchmarks section, plus restructured the README with a "Features" list, a proper "Contributing" section (not just a notes list), and an "Acknowledgments" section | Embed the dashboard's live React `BarChart` component, or a screenshot of it | A README needs a static image, not a live app; a screenshot goes stale the moment `bench.json` changes and can't be regenerated without a browser. A small generator script that reads the same JSON file and re-emits SVG on demand keeps the images honest and reproducible with `node scripts/gen-bench-charts.mjs`, no headless-browser dependency. Retuned for a light surface (`#fcfcfb`) since GitHub's README preview defaults to light regardless of viewer theme, unlike the dashboard's dark-only theme; re-validated the categorical color pair for that surface with the `dataviz` skill's script (`#2f6fb0`/`#c15f1d`, all six checks pass). Caught and fixed two bugs while building it: `Number.toLocaleString()` with no explicit locale silently used this machine's OS locale (Indic digit grouping, "2,00,000") instead of the standard thousands grouping a US/international audience expects -- fixed with `toLocaleString('en-US')`; and the tallest bar's value label initially sat almost flush against the chart subtitle at high dynamic range (N=1000's exact-mode fee bar reaches near the top) -- fixed by reserving a fixed headroom fraction (14%) above the scaled bar height rather than letting the bar fill the full plot area |
| 2026-09-12 | Rewrote all four SDK package READMEs (`core`, `escrow-client`, `x402-avm-batch`, `settler`) plus added one for the new `apps/settler` service, as devtool-grade documentation (install command, quickstart with real accurate code, full API reference table, testing section) instead of engineering-note-style summaries; added an "SDK packages" section to the top-level README linking to each | Leave the existing short READMEs from the P6 packaging pass | User-facing ask: "the sdk part should speak for itself... its a devtool thing". Cross-checked every code sample against the actual exported type signatures before writing it (not from memory) and found two real doc bugs while doing so: `settler/README.md`'s example used field names `unclaimedThresholdAtomic`/`maxAgeMs` that don't exist on `ClaimPolicyConfig` (the real fields are `thresholdAtomic`/`periodicMs`) -- copy-paste drift from an early draft that would have been a confusing runtime `undefined` for anyone following the old README; and `x402-avm-batch/README.md`'s client example invented config fields (`network`, `payerAuthorizer`, `signVoucher`) that don't exist on `BatchSettlementAvmClientConfig` -- the real shape (`payerAddress`, `deployment`, `buildDepositGroup`, `depositMultiplier`) manages the session key internally rather than taking one from the caller. Rewrote both against the actual source and against `apps/demo-agent`'s real working wiring, not against memory or a guessed API shape |
| 2026-09-12 | **MainNet deployment approved.** Per the repository owner's explicit instruction in this session, deploying `X402BatchSettlement` to Algorand MainNet, funded from their own wallet (~4 ALGO available, ~0.6 ALGO required). This overrides CLAUDE.md §4 P7's default ("the escrow contract stays off MainNet until reviewed") under that same section's own escape hatch: "Optional (only if docs/DECISIONS.md records team approval)" | Wait indefinitely for a formal external security audit before any MainNet deployment | The owner explicitly acknowledged, when asked directly, that this contract has passed offline tests (25), real LocalNet transactions (6 + escrow-client/settler suites), and a 24/24 adversary suite, but has had **no external/formal security audit**, and chose to proceed anyway understanding that MainNet funds have no outside review backing them. Before writing the deploy script: reran the full offline+LocalNet contract suite (31/31 passed) and the adversary suite (24/24 passed) fresh, as the final correctness gate, per the owner's request to "optimize" before deploying -- attempting to recompile the contract from source to double-check bytecode reproducibility hit an unrelated Windows Python-launcher PATH quirk (puyapy's subprocess `python` invocation resolves to the Store-alias stub rather than the real interpreter via native Windows process creation, distinct from bash's own PATH resolution which finds it fine); deliberately did not chase that further or touch contract.py itself, since introducing any contract change this close to a real-money deploy would add risk, not reduce it -- the already-committed, already-tested ARC-56 artifact is what gets deployed, unmodified. New `contracts/scripts/deploy_mainnet.py` mirrors the proven `deploy_testnet.py` flow exactly (idempotent, generates a fresh local deployer keypair and only ever prints its address -- never asks for, logs, or touches the owner's existing wallet mnemonic/private key) but with three deliberate changes for a real-money target: (1) no mock-asset creation -- always opts into real Circle USDC on MainNet, ASA 31566704, matching `@turnstile/core`'s `USDC_ASA_ID.mainnet`; (2) no payer/receiver sub-account carving or test USDC transfer -- this script's only job is getting the contract itself live, opening a real channel is a separate, deliberate step requiring actual USDC the owner would need to acquire separately from the 4 ALGO gas/MBR budget; (3) an additional explicit `MAINNET_DEPLOY_CONFIRM=yes` gate beyond just having a funded deployer, so a stray rerun after funding can't deploy by accident. Deployer address generated this session: funding instructions were printed with a full cost breakdown (0.1 ALGO min-balance floor + 0.3 ALGO app MBR + ~0.2 ALGO fee headroom = 0.6 ALGO total); the address and its mnemonic live only in the gitignored `contracts/.env.mainnet`, confirmed via `git check-ignore` before proceeding |
| 2026-09-12 | **Deployed to MainNet.** App id `3703998610`, app account `53VIOBJWKTCMEWC5SFN4AI5PF2PEICJWUTPKAFHIL44DMZXF43C4UY5CL4`, opted into real Circle USDC (asset `31566704`) | — | Owner funded the generated deployer with 0.9 ALGO; `deploy_mainnet.py` with `MAINNET_DEPLOY_CONFIRM=yes` deployed the app and funded/opted it into USDC in one run (actual cost: ~0.305 ALGO, cheaper than the 0.6 ALGO estimate -- deployer address retains 0.595 ALGO). Verified independently against the public MainNet API (`https://mainnet-api.algonode.cloud`), not just the script's own reported success: fetched the app directly and confirmed its approval-program bytecode contains the `x402-avm-bs-channel-v1`/`x402-avm-bs-voucher-v1` domain-separation prefixes (rules out an app-id mismatch or a different contract entirely), and fetched the app account directly and confirmed `assets: [{asset-id: 31566704, amount: 0}]` -- opted in, nothing deposited. No channel has been opened and no USDC has moved; that's a separate, deliberate step requiring a payer, a receiver, and real USDC holdings, none of which this session has |
| 2026-09-12 | Set up MainNet demo payer/receiver accounts (`contracts/scripts/mainnet_demo_setup.py`) funded from the deployer's own leftover ALGO rather than asking the owner for more, and added a "Live deployments & transaction log" section to the README with real transaction ids for both TestNet and MainNet, fetched fresh from public indexers (`mainnet-idx.algonode.cloud`/`testnet-idx.algonode.cloud`) rather than copied from a script's own stdout | Trust the deploy scripts' own printed summaries as the record | The owner asked to "log those transactions as I need to show transacts" -- a script exiting 0 isn't itself proof of an on-chain transaction; fetching each transaction id independently from a public indexer (and checking the returned link resolves with a 200) means the README's transaction log is externally verifiable by anyone, not just self-reported. Found a real gap while doing this: the deployer's own minimum balance is 0.2 ALGO once it has created an app (not the 0.1 ALGO floor an account with no created apps needs), which `deploy_mainnet.py`'s original 0.6 ALGO estimate didn't account for -- the first `mainnet_demo_setup.py` run left the deployer holding 344,000 microALGO but needing to keep 200,000 as its own floor, so funding both a payer (250,000) and receiver (230,000) from the remaining ~144,000 spendable failed partway through (payer succeeded, receiver did not). Owner sent an additional 0.5 ALGO to the same deployer address to cover the shortfall, after which both demo accounts were funded and opted into MainNet USDC (asset 31566704) successfully. The owner separately clarified they hold USDC on Ethereum, not Algorand -- these are different token instances on different chains despite the shared "USDC" name and Circle issuer, and moving value between them is a cross-chain bridge/exchange operation, which is a financial transfer I'm not able to execute on the owner's behalf under any circumstances; the demo payer therefore remains funded and ready but empty of USDC as of this entry, and the deposit/claim/settle rows in the README's MainNet transaction table are explicitly left for a future update rather than pre-populated |
